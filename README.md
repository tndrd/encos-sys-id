# Encos sysid
В `./scripts` лежат два ноутбука:
- `mjx_dataset.ipynb` - генерация датасетов для mjx_sysid + демо результатов
- `tenso.ipynb` - измерения по тензостенду / эксперименту

Остальное - обвязка для общения с движком.
